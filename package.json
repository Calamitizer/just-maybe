{
  "name": "maybe",
  "version": "1.0.0",
  "main": "dist/maybe.js",
  "types": "dist/maybe.d.ts",
  "author": "Alex Ruble",
  "license": "MIT",
  "scripts": {
    "lint": "tslint -c tslint.config.js -p tsconfig.json",
    "tslint-check": "tslint-config-prettier-check tslint.config.js",
    "format": "prettier --config ./prettier.config.js --write \"**/*.ts\"",
    "compile": "tsc",
    "test": "ava || exit 0",
    "build": "yarn format && yarn compile && yarn test"
  },
  "devDependencies": {
    "ava": "1.0.0-beta.5.1",
    "prettier": "^1.14.2",
    "ts-node": "^7.0.1",
    "tslint": "^5.11.0",
    "tslint-config-prettier": "^1.15.0",
    "typescript": "^3.0.1"
  }
}
